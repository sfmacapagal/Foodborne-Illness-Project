Use this directory to see how the data were cleaned.

* `01-clean-outbreaks-data.R` is the original cleaning and summarization of the data
* `02-clean-prediction-data.R` is the cleaning of the validation set